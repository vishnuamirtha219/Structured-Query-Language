Indexing in SQL
  used to create index and retreive data more quickly
  to make the search faster and easy

CREATE INDEX Syntax
Creates an index on a table. Duplicate values are allowed:

      CREATE INDEX index_name
      ON table_name (column1, column2, ...);
